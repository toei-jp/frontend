import { LibphonenumberFormatPipe } from './libphonenumber-format.pipe';

describe('LibphonenumberFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LibphonenumberFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
